# Find `@export` tags in code regions

Find `@export` tags in code regions

## Usage

``` r
has_export_tag(ref)
```

## Arguments

- ref:

  The code region `srcref` to search.

## Value

`TRUE` if the given region is annotated with a `@export` tag, `FALSE`
otherwise.
